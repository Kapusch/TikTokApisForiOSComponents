# Samples

`Kapusch.TikTok.iOS.Sample` documents the smallest UIKit integration surface without committing credentials.

Copy its template values locally, register a TikTok development application, and provide real Photos local identifiers before runtime testing. CI validates the package itself because TikTok editor handoff requires an approved app and a physical device.
